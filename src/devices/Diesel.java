package devices;

public class Diesel extends Car {

    public Diesel(String producer, String model, String color, double value, int yearOfProduction) {
        super(producer, model, color, value, yearOfProduction);
    }
}
